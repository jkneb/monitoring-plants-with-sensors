import { formatDateFromNow } from '../../../helpers/format-date-from-now';
import { module, test } from 'qunit';

module('Unit | Helper | format date from now');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatDateFromNow(42);
  assert.ok(result);
});

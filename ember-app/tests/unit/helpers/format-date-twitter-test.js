import { formatDateTwitter } from '../../../helpers/format-date-twitter';
import { module, test } from 'qunit';

module('Unit | Helper | format date twitter');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatDateTwitter(42);
  assert.ok(result);
});

import { formatValue } from '../../../helpers/format-value';
import { module, test } from 'qunit';

module('Unit | Helper | format value');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatValue(42);
  assert.ok(result);
});
